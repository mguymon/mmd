require 'spec/spec_helper.rb'
 
require 'access'

describe Access do
  #fixtures :permissions, :clients, :accounts, :accesses
  
  before(:each) do
    #@access = Access.new
  end

  it "have a permission" do
    #Access.find( 1 ).permission.name.should eql("read")
  end
    
end

